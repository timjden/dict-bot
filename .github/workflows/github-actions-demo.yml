name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: sshpass -p ${{secrets.SERVER_PASSWORD}} scp * root@143.198.131.75:/dict-bot
